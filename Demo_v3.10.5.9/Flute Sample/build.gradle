// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.0'
}
}

allprojects {
    repositories {
        google()
        jcenter()
        flatDir {
            dirs 'libs'
        }
    }
}

apply plugin: 'com.android.application'


dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation project(':unity-android-resources')

    implementation 'com.android.support:support-v4:28.0.0'

    //Admob
    implementation 'com.google.android.gms:play-services-ads:16.0.0'

    //Facebook
    implementation(name: 'AudienceNetwork', ext: 'aar')
    implementation 'com.android.support:recyclerview-v7:28.+'

    // IronSource
    implementation files('libs/mediationsdk-6.9.1.jar')

    // Unity Ads
    implementation(name: 'unity-ads', ext: 'aar')

    // Vungle
    implementation files('libs/publisher-sdk-android-6.4.11.aar')
    // Required Third-party Dependencies
    implementation files('libs/converter-gson-2.5.0.jar')
    implementation files('libs/gson-2.8.2.jar')
    implementation files('libs/logging-interceptor-3.12.0.jar')
    implementation files('libs/okhttp-3.12.0.jar')
    implementation files('libs/okio-1.15.0.jar')
    implementation files('libs/retrofit-2.5.0.jar')
    implementation 'com.google.android.gms:play-services-basement:16.0.0'
    implementation 'com.google.android.gms:play-services-ads-identifier:16.0.0'

    //tradplus
    implementation files('libs/tradplus-sdk-android.jar')
    implementation files('libs/tradplus-unity.jar')


}

android {
    compileSdkVersion 28
    buildToolsVersion '28.0.3'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 28
        applicationId 'com.flute.sample'
        ndk {
            abiFilters 'armeabi-v7a', 'x86'
        }
        versionCode 5401
        versionName '3.10.5.2'
    }

    lintOptions {
        abortOnError false
    }

    aaptOptions {
        noCompress = ['.unity3d', '.ress', '.resource', '.obb']
    }

    signingConfigs {
        release {
            storeFile file('/Users/sainase/KeyStore/user.keystore')
            storePassword ''
            keyAlias '1'
            keyPassword ''
        }
    }

    buildTypes {
        debug {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            jniDebuggable true
        }
        release {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            signingConfig signingConfigs.release
        }
    }

    packagingOptions {
        doNotStrip '*/armeabi-v7a/*.so'
        doNotStrip '*/x86/*.so'
    }

    bundle {
        language {
            enableSplit = false
        }
        density {
            enableSplit = false
        }
        abi {
            enableSplit = true
        }
    }
}
